name: ghostfolio_dev
services:
  postgres:
    extends:
      file: docker-compose.yml
      service: postgres
    ports:
      - ${POSTGRES_PORT:-5432}:5432

  redis:
    extends:
      file: docker-compose.yml
      service: redis
    ports:
      - ${REDIS_PORT:-6379}:6379

volumes:
  postgres:
